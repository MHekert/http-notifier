const { notificationOptions } = require('./notification');
const { acceptedOptions } = require('./accepted');
const { examples } = require('./examples');
const { rabbitConnectionOptions } = require('./rabbitConnectionOptions');

module.exports = {
	notificationOptions,
	acceptedOptions,
	examples,
	rabbitConnectionOptions,
};
